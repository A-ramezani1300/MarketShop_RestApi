# --- تنظیمات دیتابیس PostgreSQL ---
# این مقادیر توسط سرویس 'postgres' در docker-compose.yml استفاده می‌شوند
# و همچنین توسط Django برای اتصال به دیتابیس.
POSTGRES_DB=MarketShop_RestApi  # نام دیتابیس شما (باید با settings.py مطابقت داشته باشد)
POSTGRES_USER=postgres          # نام کاربری دیتابیس
POSTGRES_PASSWORD=newpassword   # رمز عبور دیتابیس

# --- تنظیمات هاست و پورت دیتابیس برای Django ---
# 'postgres' نام سرویس دیتابیس در docker-compose.yml است.
# Django از این نام برای پیدا کردن کانتینر دیتابیس در شبکه داخلی Docker استفاده می‌کند.
DB_HOST=postgres
DB_PORT=5432

# --- تنظیمات Celery Broker و Result Backend ---
# این مقادیر به Celery (worker و beat) می‌گویند که چگونه به Redis متصل شوند.
# 'redis' نام سرویس Redis در docker-compose.yml است.
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# --- تنظیمات Django ---
# DJANGO_DEBUG: True برای محیط توسعه. در محیط پروداکشن باید False باشد.
DJANGO_DEBUG=True

# DJANGO_ALLOWED_HOSTS: لیست هاست‌هایی که Django اجازه دسترسی به آن‌ها را می‌دهد.
# در توسعه می‌توانید localhost و 127.0.0.1 را قرار دهید. در پروداکشن، آدرس‌های دامنه خود را اضافه کنید.
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# DJANGO_SECRET_KEY: یک کلید امنیتی بسیار مهم برای Django.
# حتماً این را با یک رشته تصادفی و قوی جایگزین کنید!
# می‌توانید از ابزارهایی مانند 'python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"' برای تولید آن استفاده کنید.
#DJANGO_SECRET_KEY=your_django_secret_key_here_please_change_this_to_a_random_string

